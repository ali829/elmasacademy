name: CI -> Deploy to My website
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Upload from public_html via FTP
        uses: airvzxf/ftp-deployment-action@latest
        with:
          server: 'ftp.mahalkoom.com'
          user: 'elmasAcademy@elmasacademy.com'
          password: 'W=WcU0uxrwMM'
          local_dir: "./public_html/elmasacademy"
          delete: "false"